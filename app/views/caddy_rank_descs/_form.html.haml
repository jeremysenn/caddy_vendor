= simple_form_for(@caddy_rank_desc) do |f|
  = f.error_notification

  .form-inputs
    =# f.collection_select :ClubCompanyID, current_user.clubs.order(:CourseName), :ClubCourseID, :CourseName, {selected: params[:club_id]}
    = f.input :ClubCompanyID, :as => :hidden, :input_html => { :value => "#{@caddy_rank_desc.new_record? ? @club.id : @caddy_rank_desc.club.id}" }
    = f.input :RankingAcronym, as: :string
    = f.input :RankingDescription, as: :text

  .form-actions
    = link_to 'Cancel', :back, class: 'btn btn-secondary'
    = f.button :submit, class: 'btn btn-primary'
