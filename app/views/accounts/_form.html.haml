= simple_form_for(@account) do |f|
  = f.error_notification

  .form-inputs
    = f.input :MinBalance, as: :string

  .form-actions
    = link_to 'Cancel', :back, class: 'btn btn-secondary'
    = f.button :submit, "Save", class: 'btn btn-primary'
